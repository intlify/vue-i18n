import { DirectiveBinding, VNode } from 'vue'

export function hook (el: any, binding: DirectiveBinding, vnode: VNode, prevVNode: VNode): void { // eslint-disable-line
  // TODO: v-t directive
  // ...
}
