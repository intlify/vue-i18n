// import { createScanner } from './scanner'

type Tokenizer = Readonly<{
}>

export function createTokenizer (source: string): Tokenizer {
  // const _scanner = createScanner(source)

  return Object.freeze({
    // TODO
  })
}
